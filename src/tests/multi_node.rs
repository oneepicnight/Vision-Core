#[cfg(test)]
mod tests {
    /// Placeholder: multi-node integration tests.
    ///
    /// These tests will spin up multiple in-memory node instances and verify
    /// peer discovery, block propagation, and chain convergence.
    #[tokio::test]
    async fn multi_node_placeholder() {
        // TODO: implement multi-node test harness.
    }
}
